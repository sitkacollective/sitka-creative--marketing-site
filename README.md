# Sikta Creative

<img src="src/logo.svg">

## Deployment

This site is deployed via Cloudflare and a GitHub webhook-based deployment flow (not AWS).
The Terraform files in `pipeline/terraform/` are legacy and are no longer required.